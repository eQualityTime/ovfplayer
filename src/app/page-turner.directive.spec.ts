import { PageTurnerDirective } from './page-turner.directive';

describe('PageTurnerDirective', () => {
  it('should create an instance', () => {
    const directive = new PageTurnerDirective();
    expect(directive).toBeTruthy();
  });
});
